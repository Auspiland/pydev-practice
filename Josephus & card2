def josep(n,k):
  a=list(range(1,n+1))
  m=0
  while(len(a)):
    m=(m+k-1)%len(a)
    print(a.pop((m)))


def card(n):
  a=list(range(1,n+1))
  while(len(a)-1):
    print("poped: ",a.pop(0))
    a.append(a[0])
    a.pop(0)
  print(a[0])
